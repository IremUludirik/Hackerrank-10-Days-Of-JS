function main() {
    
    const PI= Math.PI;
    let readPI = Number(readLine(PI));
    
    let radius = ""
    let r= parseFloat(radius);
    
    console.log((readPI**2)*PI)
    console.log(2*PI*readPI)
    
}